pipeline {
  agent any
  tools {
    nodejs 'nodeJS2390'
  }
  stages {
    stage ("npm dependencies install") {
      steps {
        echo "install dependencies..."
        sh 'npm install --no-audit'
      }
    }
  }


      
    }
    
  }
}
